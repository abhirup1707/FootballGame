# Local development uses http://localhost:5000 automatically.
# In production, set this in the FRONTEND host's environment variables, then
# rebuild/redeploy it. Use your Render backend URL without a trailing slash.
VITE_SOCKET_URL=https://football-draft-server.onrender.com
# VITE_API_URL is also supported if you prefer a separate REST API setting.
